# ═══════════════════════════════════════════════════════════════════════════════
# CampusArena — Python Dependencies
# ═══════════════════════════════════════════════════════════════════════════════

# Core
Django>=6.0,<7.0
python-decouple>=3.8
gunicorn>=22.0

# Image handling (required for Event.banner ImageField)
Pillow>=10.0

# Database
psycopg[binary]>=3.1
mysqlclient>=2.2

# Static files (production)
whitenoise>=6.6

# Real-time (Django Channels + WebSocket support)
channels>=4.0
channels-redis>=4.2
daphne>=4.1

# HTML sanitization (XSS prevention for rich text)
bleach>=6.1

# Calendar export (.ics file generation)
icalendar>=5.0

# Excel export
openpyxl>=3.1

# Filtering
django-filter>=24.0

# Development / Testing
coverage>=7.4
factory-boy>=3.3
pytest-django>=4.8
ruff>=0.3
black>=24.0
isort>=5.13

# Security scanning
safety>=3.0
bandit>=1.7
